export const selectCategoriesSummary = ({summary}) => summary.summary.categoriesSummary;
export const selectIncomeSummary = ({summary}) => summary.summary.incomeSummary;
export const selectExpenseSummary = ({summary}) => summary.summary.expenseSummary;
export const selectPeriodTotal = ({summary}) => summary.summary.periodTotal;
